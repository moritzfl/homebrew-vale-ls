# vale-ls Homebrew tap

This tap provides Homebrew formulae for installing `vale-ls`.

## Add the tap

```sh
brew tap moritzfl/vale-ls
```

## Install vale-ls

```sh
brew install vale-ls
```

## Install a specific version

```sh
brew install vale-ls@0.4
```
